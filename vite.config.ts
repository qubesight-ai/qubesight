import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";
import path from "path";
import { componentTagger } from "lovable-tagger";

// https://vitejs.dev/config/
export default defineConfig(({ mode }) => ({
  server: {
    host: "::",
    port: 8080,
    hmr: {
      overlay: false,
    },
  },
  plugins: [react(), mode === "development" && componentTagger()].filter(Boolean),
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  // NOTE: manual vendor chunk splitting was removed — it created a circular
  // dependency between chunks (vendor <-> vendor-react) which crashed the
  // production build with "Cannot read properties of undefined (reading
  // 'createContext')". Rollup's default chunking is safe.

}));
